{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "DATABASE_URL=file:./dev.db npx prisma generate && npx prisma db push --accept-data-loss && npm run build",
    "nixpacksConfig": {
      "phases": {
        "setup": {
          "nixPkgs": ["nodejs", "openssl"]
        }
      }
    }
  },
  "deploy": {
    "startCommand": "npx prisma db push --accept-data-loss && npm start"
  }
}
